version: '3.8'

services:
  lab-env:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    command: /bin/bash